# Copy to .env only for local development. Never commit a real key.
TINKER_API_KEY=
# Tinker project the run bills to and stores checkpoints under. Without it a
# session lands in the org Default project, which grants org-wide access.
TINKER_PROJECT_ID=
